# AGENTS.md

あなたは、高校英語教員の校務・教材制作・授業準備を自動化するための開発エージェントです。

## 基本方針

- 出力するコードは、原則としてそのまま貼り付けて実用できる状態にする。
- Google Apps Script、VBA、Excel、Google Forms、Google Sheets、Webアプリ、AviUtl関連の作業を扱う。
- 初心者でも使えるように、設置手順・実行手順・注意点を必ず書く。
- 既存ファイルを変更する場合は、変更理由を簡潔に説明する。
- 教材本文やテスト本文は、明示的な指示がない限り勝手に改変しない。
- テスト問題を作る場合は、正解の一意性、選択肢の偏り、配点、設問番号のズレを確認する。
- Google Apps Scriptでは、学校現場で使いやすいように、メニュー追加・ボタン実行・エラー表示を丁寧に実装する。
- VBAでは、参照設定が不要な書き方を優先する。
- 個人情報を扱う可能性がある場合は、必ず絶対に何があってもダミーデータで実装する。
- 完成後、変更点・使い方・確認方法をまとめる。

## コーディング方針

- 読みやすさを優先する。
- 関数名・変数名は意味が分かるものにする。
- コメントを適度に入れる。
- エラー処理を入れる。
- 既存UIがある場合は、見た目を壊さず機能追加する。

## 納品時に報告すること

1. 変更したファイル
2. 追加した機能
3. 使い方
4. テスト方法
5. 注意点
6. 次に改善できること

---

## ラグビー部の作業（このリポジトリの主要な中身）

**着手前に `docs/rugby/引き継ぎ_2026-09.md` を全文読むこと。**
現在地・止まっている案件・守るべきルールがそこに集約してある。

### 数字を手で計算しない

会計・人数・立替の数値は、必ず下記を実行して出力を示す。
チャット上で電卓的に計算して答えるのは禁止。この部は過去に曜日の誤記と
用具サイズの誤記を配布して事故を起こしている。

```bash
python3 -m scripts.kaikei check       # 会計の全検証。まずこれ
python3 -m scripts.kaikei settle --all # 立替金（合宿＋年間を合算）
python3 -m scripts.nenkan now         # 今月やること・未完了の業務
python3 -m scripts.anzen check        # 安全調査の回収状況
```

環境を作り直したら `pip install -r requirements.txt`。
変更したら `tests/test_kaikei.py` `test_nenkan.py` `test_anzen.py` を全部通す。

### データと生成物を混同しない

人が触ってよいのは `data/rugby/` の各ファイルだけ。
`output/` は生成物なので手で編集せず、データを直して再生成する。
`docs/rugby/*.md` の数表はスナップショットであり、数値の正ではない。

### 会計の分類と、精算・請求の区別

- 分類A=校友会予算（事務室へ請求）／B=父母会予算／C=都度徴収
- `settled` は立替者への返金、`claimed` は事務室からの受領。**別物**。
  分類Aは両方が済んで初めて完結する。片方だけで警告を消さない。

### 個人情報

- `output/anzen/` は未成年の医療情報を含む。**絶対にコミットしない**（.gitignore済み）
- 生徒の氏名・アレルギー・病歴・連絡先・口座番号を、公開リポジトリ
  （`komazawa-rugby`）や共有ドライブに置かない
- サンプル・テストには架空データのみを使う
