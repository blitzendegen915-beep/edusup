# AGENTS.md

このリポジトリで作業するエージェント（Codex / Claude Code など）への指示書。
**作業を始める前に、このファイルを最後まで読むこと。**

---

## 1. このリポジトリは何か

高校英語教員（駒澤大学高等学校・占部涼也）の校務を自動化するリポジトリ。
中身の大半は **ラグビー部の運営システム**（会計・年間業務・安全管理）で、
残りが教材制作や授業準備のスクリプト。

利用者はプログラマではない。**コードを読ませる前提にせず、
コマンド1つで結果が出る状態にする。**

## 2. 最初に読むもの

1. **`docs/rugby/引き継ぎ_2026-09.md`** — 現在地、3つのシステムの使い方、
   止まっている案件、部の会計ルール。**これを読まずに会計に触らない。**
2. `docs/rugby/基礎データ.md` — 学校・顧問・口座・取引先・単価の一元データ。
   **連絡先や金額を記憶で書かず、必ずここを参照する。**

---

## 3. 絶対に守ること

守れないと事故になるもの。判断に迷ったら手を止めて利用者に聞く。

### 3-1. 数字を手で計算しない

金額・人数・立替額は、**必ずコマンドを実行してその出力を示す**。
会話の中で電卓的に計算した数値を答えとして出さない。

この部は過去に、曜日の誤記（火曜と書いたが実際は水曜）と
用具サイズの誤記（10名分）を保護者に配布して事故を起こしている。
検算はシステムの仕事であって、人間や言語モデルの暗算の仕事ではない。

### 3-2. 個人情報

- `output/anzen/` は**未成年の医療情報**（アレルギー・病歴）を含む。
  **絶対にコミットしない。** `.gitignore` 済みだが、意図的に外さない。
- 生徒の氏名・アレルギー・病歴・緊急連絡先・銀行口座番号を、
  **公開リポジトリ `komazawa-rugby` や共有ドライブに置かない。**
  あちらは GitHub Pages で一般公開される別リポジトリ。
- サンプル・テストデータは**必ず架空**にする（例: 見本 太郎）。
  実在の生徒のデータをテストに使わない。

### 3-3. データと生成物を混同しない

- 人が触ってよいのは **`data/rugby/` の各ファイルだけ**。
- **`output/` は生成物。手で編集しない。** データを直して再生成する。
  手で直すとデータと生成物が食い違い、次の計算が壊れる。
- `docs/rugby/*.md` の数表は書いた時点のスナップショットであり、
  **数値の正ではない**。正は `data/rugby/`。

### 3-4. 既存の書類の様式を勝手に変えない

保護者向けの文書は、昨年の様式・費目名をそのまま踏襲する。
保護者が見慣れた形式を崩さない。様式は
`docs/rugby/2026夏合宿_会計記録.md` の「昨年の様式と前例」に記録してある。

---

## 4. リポジトリの地図

```
data/rugby/          唯一の入力。人が触るのはここだけ
  roster-2026.csv           部員名簿（参加可否・合流日・合流食）
  camp-2026-summer.yml      合宿の日程・単価・大人の帯同・徴収額
  expenses-2026-summer.csv  合宿の支出（領収書番号 rNNN）
  ledger-2026.csv           合宿以外の年間支出（領収書番号 yNNN）
  annual-tasks.yml          年間業務16件
  task-log-2026.csv         年間業務の完了記録
  prior-year-misc-2025.csv  昨年の雑費36件（比較の基準）
  survey-sample.csv         安全調査のサンプル（架空データのみ）

scripts/
  kaikei/   会計（人数集計・収支・立替・会計報告書の生成・検証）
  nenkan/   年間業務（今月やること・未完了の検出）
  anzen/    安全管理（緊急時対応シート・アレルギー一覧・未回答者）
  create_*.py  個別の書類生成スクリプト（旧方式。順次 kaikei 等へ統合したい）

docs/     Obsidian vault としても開ける。ホーム.md が入口
tests/    test_kaikei.py / test_nenkan.py / test_anzen.py
output/   生成物。手で編集しない
.claude/skills/   rugby-camp / rugby-check / rugby-doc / rugby-kaikei /
                  rugby-mail / rugby-order（作業の型が書いてある）
```

## 5. 作業の進め方

```bash
pip install -r requirements.txt   # 環境を作り直したら最初に

python3 -m scripts.kaikei check        # 会計の全検証。会計に触る前に必ず
python3 -m scripts.kaikei settle --all # 立替金（合宿＋年間を合算）
python3 -m scripts.kaikei balance      # 収支
python3 -m scripts.kaikei headcount    # 日別・食事別の人数
python3 -m scripts.kaikei ledger       # 年間経費を会計分類別に
python3 -m scripts.kaikei report       # xlsx一式を output/ に生成
python3 -m scripts.kaikei add ...      # 領収書を1件追加
python3 -m scripts.kaikei claim y001   # 事務室から受領したら記録

python3 -m scripts.nenkan now          # 今月やること・未完了の業務
python3 -m scripts.nenkan done <id>    # 終わったら記録

python3 -m scripts.anzen check         # 安全調査の回収状況
python3 -m scripts.anzen generate      # 緊急時対応シート等を生成
```

手順:

1. `check` と `nenkan now` を実行して現在地を把握する
2. データを直す（`data/rugby/` のみ）
3. **テストを全部通す** — `python3 tests/test_kaikei.py` `test_nenkan.py` `test_anzen.py`
4. 再度 `check` を実行し、警告が増えていないか確認する
5. 生成物が要るなら `report` / `generate` で作り直す
6. コミットしてプッシュする

### テストの扱い

- 既存のテストを**期待値だけ書き換えて通す、ということをしない**。
  数値が変わったなら、なぜ変わったかを説明してからテストを直す。
- 金額そのものをテストにハードコードしない。領収書を1件足すたびに
  壊れるテストは脆い。**判定の振る舞い**を検証する。
- テストは一時ディレクトリで動かし、`data/` の実データを壊さない。

### コミット

- 日本語で書く。**何をしたかだけでなく、なぜそうしたかを書く。**
- 数値が変わる変更は、変わった理由と差分の内訳を本文に残す。
- 作業ブランチで作業する。データファイルと生成物を同じコミットに
  混ぜると差分が読めなくなるので、可能なら分ける。

---

## 6. ラグビー部の業務知識

### 会計分類

| 分類 | 内容 | 財源 |
|---|---|---|
| A | 大会参加費・協会登録費 | 校友会予算。**事務室へ請求して受け取る** |
| B | メディカル・補食・コーチ謝礼・対戦校への手土産 | 父母会予算 |
| C | 合宿費・ウェア代 | 各家庭から都度徴収 |

### 精算と請求は別物（間違えやすい）

- **`settled`** … 立て替えた先生に部がお金を返したか（部 → 先生）
- **`claimed`** … 校友会予算を事務室から受け取ったか（事務室 → 部）

分類Aは**両方が済んで初めて完結する**。先生への返金が終わっても、
事務室へ請求しなければ部がその金額を失う。
`claimed=yes` にするのは、事務室から実際に入金があったときだけ。

### 口座を取り違えない

- 三菱UFJ 世田谷支店 普通0887504 … 各家庭からの振込・業者への支払い
- 三井住友 桜新町支店 普通6677190 … 校友会・父母会予算の入金

### 別リポジトリ

部の公開サイトは **`komazawa-rugby`**（Astro製・GitHub Pages）。
**このリポジトリとは絶対に混ぜない。** 3-2 を参照。

---

## 7. 校務全般（ラグビー部以外）

- 扱う領域: Google Apps Script、VBA、Excel、Google Forms、
  Google Sheets、Webアプリ、AviUtl。
- 出力するコードは、そのまま貼り付けて実用できる状態にする。
- 初心者でも使えるよう、設置手順・実行手順・注意点を必ず書く。
- 教材本文・テスト本文は、明示的な指示がない限り勝手に改変しない。
- テスト問題を作る場合は、正解の一意性、選択肢の偏り、配点、
  設問番号のズレを確認する。
- Google Apps Script では、メニュー追加・ボタン実行・エラー表示を
  丁寧に実装する。VBA では参照設定が不要な書き方を優先する。
- 読みやすさを優先し、意味の分かる名前を使い、エラー処理を入れる。
- 既存UIがある場合は、見た目を壊さず機能追加する。

---

## 8. 報告の仕方

作業が終わったら、次を簡潔にまとめる。

1. 変更したファイル
2. 何ができるようになったか
3. 使い方（コマンド）
4. テスト結果
5. **注意点・気づいた問題**（見つけた不整合は隠さず書く）
6. 次にやると効くこと

数値を報告するときは、**コマンドの実際の出力を引用する**。
自分で計算し直した数値を書かない。

---

## 9. いま止まっている案件（2026-09-15 時点）

詳細と経緯は `docs/rugby/引き継ぎ_2026-09.md` の5節にある。
着手前に `python3 -m scripts.kaikei check` と
`python3 -m scripts.nenkan now` を叩いて最新の状態を確認すること。

1. **合宿の領収書が回収できていない**（最優先）
   昨年の雑費36件・¥164,602 に対し、今年は記録ベースで約28%。
   8/4・8/5 は支出記録がゼロ。氷と補食は毎日買っているはずで、
   10万円前後の領収書が未回収。**これが埋まらないと会計報告を締められない。**
2. **秋季大会参加費 ¥12,000 を事務室へ請求**（分類A・占部先生が立替中）
3. **2人目のコーチへの謝礼**（星野コーチには¥20,000支払済み）
4. **バスの有料道路代・駐車場代**（国際興業からの実費請求待ち）
5. **立替金の返金**（畠山先生 ¥32,675 ／ 占部先生 ¥34,694）
6. **仮置きの日付3件**（星野コーチ代・秋季大会費・文化祭雑費）
   ※星野コーチ代を 8/7 にしたため、8/7 の「支出記録なし」警告が
   消えている。8/7 の雑費領収書は依然未回収の可能性がある。
